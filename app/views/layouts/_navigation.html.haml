.navbar.navbar-default.navbar-fixed-top
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='#navbar' aria-expanded='false' aria-controls='navbar')
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      =link_to "Affirm.me", "", class: "navbar-brand"
    #navbar.collapse.navbar-collapse
      / - if logged_in?
      /   %form.navbar-form.navbar-right
      /     .form-group
      /       = link_to current_user, class: "btn btn-default form-control view-profile" do
      /         View Profile
      /         %i.glyphicon.glyphicon-user
      /       = link_to sign_out_path, class: "btn btn-info form-control" do
      /         Sign Out
      /         %i.glyphicon.glyphicon-log-out
      / - else
      %form.navbar-form.navbar-right
        .form-group
          %input.form-control(type='email' placeholder='Email')
        .form-group
          %input.form-control(type='password' placeholder='Password')
        = link_to "", class: "btn btn-info form-control" do
          %span.glyphicon.glyphicon-log-in
          Sign In

 
